
import { useEffect } from 'react';
import App from '@/App';

const Index = () => {
  useEffect(() => {
    // Any initialization logic can go here
  }, []);

  return <App />;
};

export default Index;
